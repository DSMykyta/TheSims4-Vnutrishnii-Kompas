# Annotated Tuning Copies

Copy selected raw XML files here before adding comments.

Use XML comments for local notes:

```xml
<!-- Finding: this loot applies the visible buff when the commodity enters the threshold. -->
```

Do not treat annotated files as source-of-truth for EA tuning. They are research notes.
