# Commodity Threshold Patterns

## Files inspected

- [ ] `commodity_Motive_Hunger`
- [ ] connected `buff_Motive_Hunger_*`
- [ ] useful `commodity_Hidden_*`

## Questions to answer

- Where is decay rate defined?
- How are threshold zones defined?
- How are buffs added on entering a threshold?
- How are buffs removed on leaving a threshold?
- Can this model express `Restlessness` without Python?
- What exact XML fields are reusable?

## Findings

Pending.

## Pure Tuning Gate

MVP stays tuning-only if `Restlessness` can be built with:

- XML decay rate.
- 3-4 XML threshold zones.
- XML loot/buff add on threshold entry.
- XML loot/buff remove on threshold exit.
- Value changes only from interaction outcomes or periodic decay.

## Reusable pattern for MVP

Pending.
